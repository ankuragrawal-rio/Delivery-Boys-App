name: rio_delivery_app
description: Rio Delivery App for managing delivery operations
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  riverpod: ^2.4.9
  flutter_riverpod: ^2.4.9
  
  # Navigation
  go_router: ^12.1.1
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  
  # HTTP & WebSocket
  dio: ^5.3.2
  web_socket_channel: ^2.4.0
  
  # QR Scanner & Image (temporarily disabled for web compatibility)
  # mobile_scanner: ^3.5.2
  # image_picker: ^1.0.4
  
  # Firebase (temporarily disabled for web compatibility)
  # firebase_core: ^2.24.2
  # firebase_messaging: ^14.7.9
  
  # UI & Utils
  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  # permission_handler: ^11.0.1
  url_launcher: ^6.2.1
  # cached_network_image: ^3.3.0
  # flutter_local_notifications: ^16.3.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
